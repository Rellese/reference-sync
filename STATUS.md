# STATUS.md — текущее состояние ReferenceSync

Обновлено: 2026-08-25

Перед продолжением необходимо прочитать:

1. `AGENTS.md`
2. `PROJECT.md`
3. `STATUS.md`

---

# Текущий этап

`M1 — Instagram parity`

Текущая задача:

`M1-T06A — восстановить модальное окно Configuring a Carousel with Thumbnails`

Статус задачи:

`planned — реализация ещё не начата`

---

# Последние зафиксированные точки

Последний проверенный commit:

`b10123fbc5c665ae4ee614693ec95ca2ba08903e`

Основная ветка:

`main`

Текущая документационная ветка:

`docs/update-project-roadmap`

Production-репозиторий:

`https://github.com/Rellese/reference-sync`

Завершённый Pull Request:

`#1 — Complete operation safety, recovery and duplicate protection`

Проверки после merge:

- local tests: `36/36 passed`;
- GitHub Actions: `passed`;
- локальный `main` синхронизирован;
- merge-конфликтов нет.

---

# Выполнено

## M0 — Project foundation

- [x] M0-T01 — создан `AGENTS.md`
- [x] M0-T02 — создан `PROJECT.md`
- [x] M0-T03 — создан `STATUS.md`
- [x] M0-T04 — проведена инвентаризация production-репозитория
- [x] M0-T05 — определён безопасный план очистки
- [x] M0-T06 — создана безопасная структура автоматических тестов
- [x] M0-T07 — зафиксирован baseline запуска в Eagle
- [x] M0-T08 — выполнена подтверждённая очистка после baseline

## M1 — Instagram parity

- [x] M1-T01 — сравнено формирование download target в Python и JS
- [x] M1-T02 — создан regression test для обнаруженного расхождения
- [x] M1-T03 — восстановлены discovery, типы и группировка публикаций
- [x] M1-T04 — восстановлено продолжение очереди после ошибки публикации
- [x] M1-T05A — добавлены выбор профиля браузера и проверка Instagram-аккаунта
- [x] M1-T05 — подтверждён импорт одиночной публикации из проверенного аккаунта
- [ ] M1-T05B — оптимизированы discovery, cookies и режимы скорости
- [ ] M1-T06 — проверены изображение, видео и карусель
- [ ] M1-T05B — оптимизировать discovery, cookies и режимы скорости
- [ ] M1-T06 — проверить изображения, видео, Reels и карусели
- [x] M1-T07 — подтверждена защита от повторного импорта
- [x] M1-T07A — добавлена сверка реестра с элементами Eagle
- [x] M1-T07B — удалённые из Eagle элементы снова доступны для импорта
- [x] M1-T07C — исправлена остановка активной операции
- [x] M1-T07D — добавлено восстановление после аварийного закрытия
- [x] M1-T07E — добавлена очистка jobs и staging
- [x] M1-T07F — исправлены итоговые счётчики и состояние завершённого импорта

---

# Результат M0-T06

Созданы:

- `package.json`;
- `.github/workflows/tests.yml`;
- `tests/README.md`;
- `tests/fixtures/README.md`;
- `tests/unit/core-smoke.test.js`;
- безопасные правила `.gitignore`.

Автоматически проверяются:

- распознавание сетевых ошибок;
- отличие сетевой ошибки от Instagram 429/login required;
- остановка длительного задания;
- базовый контракт реестра источников;
- отклонение неправильного source descriptor.

Результат:

`4 tests passed`

Production-логика Instagram в M0-T06 не изменялась.

В тесты не добавлены:

- cookies;
- токены;
- личные архивы;
- скачанные публикации;
- пользовательские данные.

---

# Историческое состояние до завершения M1-T05 и M1-T07

Этот раздел сохранён как baseline и описывает состояние проекта до завершения
живых проверок импорта, защиты от дубликатов и восстановления.
Актуальное состояние записано в конце документа.

## Instagram

Статус:

`implemented, not live-tested in the current recovery cycle`

Подтверждено чтением кода:

- поиск через `gallery-dl`;
- нормализация публикаций;
- staging;
- базовый импорт в Eagle;
- pause, resume и stop внутри текущего запуска.

Не подтверждено живым тестом:

- запуск интерфейса в Eagle;
- подготовка `gallery-dl` одной кнопкой;
- получение cookies из браузера;
- поиск сохранённых публикаций;
- изображения, видео, Reels и карусели;
- импорт в Eagle;
- защита от повторного импорта;
- выбранная папка Eagle;
- восстановление после перезапуска;
- очистка staging.

## Интерфейс

Статус:

`implemented partially, not verified 1:1`

Не проверены:

- точное совпадение с дизайном;
- работа всех кнопок;
- изменение размеров блоков;
- большая таблица;
- дерево папок;
- Block 6;
- модальное окно карусели.

## Другие источники

- Pinterest — skeleton, не проверен;
- Dribbble — skeleton, не проверен;
- Behance — skeleton, не проверен;
- Vimeo — skeleton, не проверен;
- X — skeleton, не проверен;
- Layers.to — не поддерживается;
- Meta Archive — не реализован.

До отдельной реализации и проверки рабочим источником считается только Instagram.

---
# Результат M0-T07

Среда:

- macOS;
- Eagle 4.0.0 build 22;
- ReferenceSync Beta 1.2;
- gallery-dl 1.32.9;
- Chrome cookies.

Подтверждено живым запуском:

- плагин открывается;
- интерфейс отображается;
- повторное открытие работает;
- Eagle API доступен;
- gallery-dl определяется;
- cookies извлекаются из Chrome;
- поиск Instagram работает;
- найдено 17 публикаций;
- скачивание запускается.

Обнаруженный блокер:

- Instagram вернул `400 Bad Request` для одной публикации;
- плагин ошибочно классифицировал ответ как обрыв связи;
- очередь не дошла до импорта в Eagle;
- импорт в Eagle не подтверждён.

Дополнительное предупреждение macOS:

- системный Python использует LibreSSL 2.8.3;
- urllib3 v2 показывает `NotOpenSSLWarning`;
- предупреждение не остановило поиск;
- совместимость необходимо проверить отдельно.

Решение:

Не исправлять симптом отдельно во время M0.

В M1 сравнить Python-эталон и JS-плагин, создать regression test
и восстановить корректную цепочку discovery → download → import.

# Решение по очистке

До успешного M0-T07 ничего из старого production-кода не удалять.

После baseline можно отдельными небольшими коммитами удалить:

- `HANDOFF.md`;
- `ecosystem.config.cjs`;
- неподтверждённые source adapters;
- неиспользуемый `gallery-source.js`;
- неработающий Layers adapter.

CSS, шрифты и иконки пока сохраняются.

---

# Исторические риски до operation-safety merge

1. Постоянный реестр импортированных публикаций не подтверждён.
2. После перезапуска возможен повторный импорт.
3. Восстановление незавершённого задания не подтверждено.
4. Выбор папки Eagle не подключён к импорту.
5. Проверка созданного элемента Eagle не подтверждена.
6. `main.js` напрямую связан с Instagram и не использует единый adapter.
7. `instagram.js` и `gallery-source.js` содержат дублирование.
8. Непроверенные источники помечены как готовые.
9. README и HANDOFF устарели.
10. Автоматические unit-тесты не заменяют живой запуск Eagle.

---

# Историческая задача M0-T07

## M0-T07 — baseline запуска в Eagle

Цель:

Проверить текущий проект без исправления ошибок и зафиксировать фактическое поведение.

Проверить:

1. импорт локального проекта в Eagle;
2. открытие окна плагина;
3. отсутствие пустого или аварийного экрана;
4. отображение основных панелей;
5. состояние проверки `gallery-dl`;
6. доступность кнопки подготовки;
7. технический журнал;
8. закрытие и повторное открытие плагина.

Во время baseline:

- не исправлять найденные ошибки;
- не удалять файлы;
- не запускать полный рефакторинг;
- записать точный текст ошибок;
- сделать снимки экрана проблемных состояний.

---

# Результат M0-T08

Удалены:

- `HANDOFF.md`;
- `ecosystem.config.cjs`.

Сохранены до безопасного рефакторинга:

- source adapters;
- `gallery-source.js`;
- assets;
- production-логика Instagram.

Проверки после очистки:

- автоматические тесты прошли;
- плагин повторно открыт в Eagle;
- интерфейс отображается;
- новых ошибок запуска нет.

Этап M0 завершён.

# Результат M1-T01

Проверена цепочка:

`discovery → normalization → download target → failure classification`

Подтверждено:

- Python и JS используют URL публикации как download target;
- внутренний `/api/v1/media/.../info/` вызывает `gallery-dl`;
- неправильное формирование media ID в JS не подтверждено;
- Instagram вернул настоящий `400 Bad Request`;
- интернет-соединение при этом не пропадало.

Подтверждённые расхождения Python и JS:

1. Python распознаёт сеть по точным шаблонам, включая `ssl error`.
2. JS использует слишком широкое слово `ssl`.
3. `NotOpenSSLWarning` поэтому ошибочно считается обрывом связи.
4. Python классифицирует обычный `400` как `COMMAND_ERROR`.
5. JS запускает для него лестницу сетевых повторов.
6. Python создаёт один защищённый cookie snapshot на пачку.
7. JS повторно читает cookies браузера при каждой попытке.

Решение:

Сначала создать обезличенные regression tests, затем отдельно
исправить классификацию и продолжение очереди.

Cookie snapshot переносить отдельной задачей после восстановления
корректной обработки ошибок.

# Результат M1-T03

Статус:

`live-tested`

Исправлена обработка результата Instagram Saved:

- вложенный `pk` автора больше не считается публикацией;
- записи компонентов объединяются по `post_id`;
- карусели сохраняют все компоненты;
- фото и видео определяются по расширению;
- вымышленные ссылки `/p/id:.../` больше не создаются;
- сохраняется исходный порядок Saved;
- лимит Recent применяется к публикациям, а не вложенным объектам.

Проверено в Eagle 4.0.0:

- запрошено 8 публикаций;
- найдено 8 публикаций;
- выбрано 8 публикаций;
- порядок совпал с Instagram Saved;
- авторы совпали;
- превью совпали;
- обнаружено 2 фото, 5 видео и 1 карусель;
- ложных строк `id:...` нет.

Автоматические тесты:

`12 passed`

Источник требований к интерфейсу и поведению компонентов:

`https://github.com/Rellese/plugin/tree/main/Full_design`

Кнопка настройки карусели и выбор отдельных компонентов ещё не подключены. Их поведение должно быть перенесено строго по файлам:

- `Full_design/Configuring the Carousel`;
- `Full_design/Configuring a Carousel with Thumbnails`;
- соответствующим файлам в `Full_design/Instruction/`.


# Точное следующее действие

Выполнить M1-T04.

Цель:

Подтвердить и зафиксировать, что ошибка одной публикации не останавливает остальные публикации в очереди.

Проверить:

1. успешные элементы до ошибки сохраняются;
2. ошибочный элемент получает собственную ошибку;
3. следующие элементы продолжают обрабатываться;
4. итог содержит отдельные списки успешных и неуспешных элементов;
5. `400 Bad Request` не запускает ожидание восстановления соединения;
6. настоящая сетевая ошибка сохраняет механизм повторных попыток;
7. остановка пользователя завершает всю очередь.

Production-код менять только после regression test.
---

# M1-T05A — результат

Статус: `completed`

Реализовано:

1. Chromium-профили читаются из `Local State`.
2. Пользователю показываются реальное имя профиля и почта.
3. Технические названия `Profile 1` / `Profile 6` скрыты.
4. При одном найденном профиле поле выбора не отображается.
5. При нескольких профилях пользователь выбирает нужный аккаунт.
6. gallery-dl получает конкретный профиль браузера.
7. Перед discovery определяется реальный авторизованный Instagram `@username`.
8. При несовпадении аккаунтов discovery блокируется.
9. Отсутствующая или просроченная Instagram-сессия блокируется.
10. Предупреждение показывается в отдельном модальном окне и не растягивает интерфейс.
11. Временный файл cookies удаляется после проверки.
12. Добавлены unit-тесты профилей, cookies, username и срока сессии.

Живая проверка:

- Chrome обнаружил 5 профилей;
- отображаются реальные названия и почты;
- Yandex с одним профилем скрывает поле выбора;
- правильный профиль определён как `@rellese26`;
- неправильный профиль определён как `@kl33.pa` и заблокирован;
- профиль без действующего входа заблокирован;
- правильный профиль нашёл 27 Saved-публикаций;
- временных cookie-файлов после операции не осталось;
- модальное предупреждение проверено в Eagle;
- интерфейс не растягивается.

Проверено:

- `npm test` — 25 passed, 0 failed;
- синтаксис изменённых JS-файлов — passed;
- Eagle 4.0.0;
- gallery-dl 1.32.9.

Не проверено:

- одиночный импорт после добавления проверки аккаунта;
- повторный импорт и защита от дубликатов;
- производительность полного discovery;
- корректность режимов Safe / Balanced / Lightning.

Следующее действие:

1. дождаться успешного GitHub Actions для M1-T05A;
2. выбрать правильный профиль `@rellese26`;
3. найти Saved-публикации;
4. импортировать одну фотографию в Eagle;
5. проверить файл и метаданные;
6. оставить оптимизацию discovery отдельной задачей M1-T05B.

---

# M1-T05B — требования к будущей оптимизации

1. Cookies извлекаются один раз на одно нажатие «Поиск».
2. Проверка аккаунта и discovery используют один временный cookie snapshot.
3. Saved discovery запускается только один раз.
4. Маленький лимит прекращает пагинацию после получения нужного количества.
5. Полный поиск продолжает текущий cursor и не начинает поиск с первой страницы.
6. Партии по 50 используются только для обработки и отображения.
7. Каждая партия не должна создавать новый процесс discovery.
8. В техническом журнале измеряется время отдельных этапов.
9. Safe использует самые длинные паузы.
10. Balanced использует более короткие контролируемые паузы.
11. Lightning не добавляет искусственных пауз ReferenceSync.
12. Режимы сравниваются по нескольким запускам, а не по одному измерению.

---

# M1-T05 — итоговая проверка импорта и безопасности операций

Статус:

`completed — ready to merge`

Проверенный commit:

`dd06ac236143836a1739487796ff35ee58ccf1fe`

Рабочая ветка:

`fix/operation-safety-recovery`

Автоматические проверки:

- `npm test` — 36 passed, 0 failed;
- синтаксис изменённых JavaScript-файлов — passed.

Подтверждено живым тестом в Eagle:

1. Одиночная фотография импортируется в Eagle.
2. После успешного импорта `active-job.json` и staging удаляются.
3. Stop завершает скачивание без зависания интерфейса.
4. Stop не создаёт ложную ошибку авторизации Instagram.
5. После Stop временные файлы удаляются.
6. После принудительного завершения Eagle manifest и staging сохраняются.
7. После повторного запуска незавершённое задание восстанавливается.
8. Восстановленный импорт завершается без повторного скачивания готовых публикаций.
9. После восстановления временные данные удаляются.
10. Реестр связывает публикации с элементами Eagle.
11. Удалённый из Eagle элемент снова становится доступен для импорта.
12. Полностью импортированные публикации повторно не добавляются.
13. Итог импорта отдельно показывает количество публикаций и элементов Eagle.
14. Когда все строки импортированы, кнопка скачивания становится неактивной.
15. Отсутствие новых публикаций больше не показывается как ошибка входа в Instagram.

Не завершено:

1. Ячейка «Тип / Карусель» не открывает окно выбора компонентов.
2. Компонент `Configuring a Carousel with Thumbnails` ещё не перенесён в рабочий интерфейс.
3. Частичный выбор элементов карусели не проверен.
4. Импорт видео и Reels не подтверждён в текущем цикле.
5. Safe / Balanced / Lightning не сравнены измерениями.
6. Производительность запуска `gallery-dl` для отдельных публикаций не оптимизирована.

Проверено после слияния в `main`:

- GitHub Actions: passed;
- локальный `main` синхронизирован с `b10123f`;
- локальные тесты: 36/36 passed.

# Следующее точное действие

1. Слить `fix/operation-safety-recovery` в `main`.
2. Переключить локальный проект на `main`.
3. Получить объединённые изменения.
4. Запустить `npm test`.
5. Проверить GitHub Actions.
6. Начать M1-T06 с восстановления модального окна
   `Configuring a Carousel with Thumbnails`.
7. После карусели перейти к M1-T05B и измерению скорости.


---

# Полная дорожная карта

## M1 — Instagram parity

Статус:

`in progress`

### M1-T06 — Carousel, video and Reel parity

- [ ] M1-T06A — восстановить окно `Configuring a Carousel with Thumbnails`
- [ ] M1-T06B — открывать окно при нажатии на ячейку карусели
- [ ] M1-T06C — показывать thumbnails компонентов карусели
- [ ] M1-T06D — позволить выбирать отдельные компоненты
- [ ] M1-T06E — сохранять исходный порядок компонентов
- [ ] M1-T06F — проверить карусель изображений
- [ ] M1-T06G — проверить смешанную карусель
- [ ] M1-T06H — проверить одиночное видео
- [ ] M1-T06I — проверить Reels
- [ ] M1-T06J — проверить публикацию без описания

### M1-T05B — Discovery and download performance

- [ ] M1-T05B-01 — измерить текущее время поиска и скачивания
- [ ] M1-T05B-02 — кэшировать данные публикаций во время поиска
- [ ] M1-T05B-03 — сохранять прямые ссылки, полученные во время discovery
- [ ] M1-T05B-04 — не выполнять повторный поиск перед скачиванием
- [ ] M1-T05B-05 — извлекать cookies один раз на операцию
- [ ] M1-T05B-06 — не запускать повторный discovery для каждой партии
- [ ] M1-T05B-07 — измерить Safe, Balanced и Lightning
- [ ] M1-T05B-08 — выбрать безопасный режим по умолчанию
- [ ] M1-T05B-09 — проверить скорость на большой выборке

### M1-T08 — Filters and Instagram collections

- [ ] M1-T08A — проверить режим `Только новые`
- [ ] M1-T08B — проверить режим `Все`
- [ ] M1-T08C — проверить режим `Последние N`
- [ ] M1-T08D — проверить Instagram Saved
- [ ] M1-T08E — проверить Instagram Collections
- [ ] M1-T08F — проверить несколько коллекций
- [ ] M1-T08G — проверить фильтры по типам публикаций
- [ ] M1-T08H — проверить большую выборку сохранённых публикаций

Критерий завершения M1:

- фото, видео, Reels и карусели работают;
- компоненты каруселей можно выбирать;
- коллекции и фильтры проверены;
- повторный импорт блокируется;
- Stop и восстановление работают;
- скорость измерена и оптимизирована;
- обязательные сценарии проверены в Eagle.

---

## M2 — Eagle import completeness

Статус:

`planned`

- [ ] M2-T01 — подключить реальное дерево папок Eagle
- [ ] M2-T02 — импортировать в выбранную папку
- [ ] M2-T03 — проверить создание и выбор новой папки
- [ ] M2-T04 — проверить редактирование имени публикации
- [ ] M2-T05 — проверить редактирование описания
- [ ] M2-T06 — проверить нумерацию публикаций и компонентов
- [ ] M2-T07 — сохранять порядок публикаций и компонентов
- [ ] M2-T08 — подтверждать создание элементов через Eagle API
- [ ] M2-T09 — согласовать реестр с фактической библиотекой Eagle
- [ ] M2-T10 — проверить частично завершённый импорт
- [ ] M2-T11 — проверить большой импорт и прогресс

---

## M3 — UI and design parity

Статус:

`planned`

- [ ] M3-T01 — провести аудит production UI и `Full_design`
- [ ] M3-T02 — восстановить структуру экранов и блоков
- [ ] M3-T03 — привести таблицу публикаций к дизайну 1:1
- [ ] M3-T04 — проверить большую таблицу и прокрутку
- [ ] M3-T05 — восстановить дерево папок
- [ ] M3-T06 — восстановить Block 6
- [ ] M3-T07 — проверить модальные окна
- [ ] M3-T08 — проверить загрузку, ошибки и пустые результаты
- [ ] M3-T09 — проверить изменение размеров блоков
- [ ] M3-T10 — провести финальное визуальное сравнение 1:1

---

## M4 — Installation and toolchain

Статус:

`planned`

- [ ] M4-T01 — проверить обнаружение `gallery-dl`
- [ ] M4-T02 — реализовать понятную проверку компонентов
- [ ] M4-T03 — реализовать подготовку одной кнопкой
- [ ] M4-T04 — исключить ручной ввод системных путей
- [ ] M4-T05 — проверить поддерживаемые браузеры
- [ ] M4-T06 — проверить обновление `gallery-dl`
- [ ] M4-T07 — добавить понятные ошибки установки
- [ ] M4-T08 — проверить первый и повторный запуск

---

## M5 — Release quality and moderation

Статус:

`planned`

- [ ] M5-T01 — расширить unit- и regression-тесты
- [ ] M5-T02 — подготовить обезличенные fixtures
- [ ] M5-T03 — провести полный live-test Instagram
- [ ] M5-T04 — протестировать macOS
- [ ] M5-T05 — протестировать Windows
- [ ] M5-T06 — проверить чистую установку
- [ ] M5-T07 — подготовить release-сборку
- [ ] M5-T08 — обновить README
- [ ] M5-T09 — подготовить документацию пользователя
- [ ] M5-T10 — подготовить документацию для модератора
- [ ] M5-T11 — проверить требования Eagle Plugin Center
- [ ] M5-T12 — выполнить финальный release checklist

---

## M6 — Additional sources

Статус:

`planned — skeleton не считается готовым источником`

- [ ] M6-T01 — завершить общий контракт source adapter
- [ ] M6-T02 — скрыть неподтверждённые skeleton-источники
- [ ] M6-T03 — реализовать Pinterest
- [ ] M6-T04 — реализовать Dribbble
- [ ] M6-T05 — реализовать Behance
- [ ] M6-T06 — реализовать Vimeo
- [ ] M6-T07 — реализовать X
- [ ] M6-T08 — реализовать дополнительные источники
- [ ] M6-T09 — отдельно исследовать Layers

---

## M7 — Meta Archive

Статус:

`not implemented`

- [ ] M7-T01 — drag and drop архива
- [ ] M7-T02 — проверить формат и структуру
- [ ] M7-T03 — безопасно распаковать архив
- [ ] M7-T04 — показать структуру архива
- [ ] M7-T05 — позволить выбрать публикации
- [ ] M7-T06 — импортировать выбранное в Eagle
- [ ] M7-T07 — защитить от повторного импорта
- [ ] M7-T08 — очистить временные данные

---

## M8 — Localizations

Статус:

`planned`

- [ ] M8-T01 — вынести строки интерфейса в ресурсы
- [ ] M8-T02 — English
- [ ] M8-T03 — Русский
- [ ] M8-T04 — Français
- [ ] M8-T05 — Español
- [ ] M8-T06 — 中文
- [ ] M8-T07 — проверить переносы и переполнение текста

---

# Актуальные риски

1. Модальное окно карусели не открывается из ячейки.
2. Выбор отдельных компонентов карусели не работает.
3. Видео, Reels и смешанные карусели не подтверждены живым тестом.
4. Скорость отдельных запусков `gallery-dl` не оптимизирована.
5. Discovery-данные и прямые ссылки не переиспользуются при скачивании.
6. Выбор настоящей папки Eagle не подтверждён.
7. Интерфейс не проверен на соответствие дизайну 1:1.
8. `main.js` остаётся крупным и тесно связан с Instagram.
9. Непроверенные источники нельзя считать готовыми.
10. Windows не проверен.
11. README и release-документация требуют обновления.
12. Unit-тесты не заменяют живую проверку Eagle.

---

# Следующее точное действие

## M1-T06A — восстановить модальное окно карусели

Перед изменением production-кода сравнить:

1. актуальный production-код;
2. Python-эталон `ReferenceSyncforOpus5`;
3. дизайн `plugin/Full_design/`;
4. текущую модель данных карусели;
5. существующие UI-компоненты;
6. обработчик нажатия на ячейку карусели.

Критерии проверки:

- ячейка карусели открывает окно;
- thumbnails соответствуют компонентам;
- компоненты можно включать и выключать;
- порядок компонентов сохраняется;
- выбор попадает в очередь импорта;
- одиночные публикации не затронуты;
- unit-тесты проходят;
- сценарий проверен в Eagle.
