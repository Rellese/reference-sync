{
  "name": "reference-sync",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/*.test.js",
    "prepare:release": "node scripts/prepare-release.mjs"
  }
}
